export default class {
    constructor(patientIdentifier, timestamp, profile, panel) {
        this.patientIdentifier = patientIdentifier;
        this.timestamp = timestamp;
        this.profile = profile;
        this.panel = panel;
    }
}