export default class {
    constructor(code, label, value, unit, lower, upper) {
        this.code = code;
        this.label = label;
        this.value = value;
        this.unit = unit;
        this.lower = lower;
        this.upper = upper;
    }
}